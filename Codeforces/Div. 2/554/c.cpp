#include <bits/stdc++.h>
using namespace std;
using i64 = long long;

i64 lcm(int x, int y) {
    return 1LL * x / __gcd(x, y) * y;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // Classcal Number Theory Problem
    

    return 0;
}