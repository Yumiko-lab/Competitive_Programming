#include <bits/stdc++.h>
using namespace std;

constexpr int dir[][2] = {
    {0, 1}, {1, 0}, {0, -1}, {-1, 0}
};

string s = "NESW";

void solve() {
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}