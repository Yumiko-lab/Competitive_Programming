#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n, m, K, xs, ys, xt, yt;
    cin >> n >> m >> K >> xs >> ys >> xt >> yt;
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}