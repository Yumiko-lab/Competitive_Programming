#include <bits/stdc++.h>
using namespace std;
using i64 = long long;

constexpr int N = 100010, MOD = 998244353;

int n, Q;
int f[N], g[N];


int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> n >> Q;
    for (int i = 1; i <= n; i++) {
        cin >> f[i];
    }
    for (int i = 1; i <= n; i++) {
        cin >> g[i];
    }

    
    
    while (Q--) {
        int op, i, x;
        cin >> op >> i >> x;
        if (op == 1) {

        } else {

        }
    }

    return 0;
}