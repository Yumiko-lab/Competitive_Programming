#include <bits/stdc++.h>
using namespace std;
using i64 = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    vector<int> a(8);
    for (auto &x : a) {
        cin >> x;
    }

    i64 ans = 0;
    for (int i = 0; i < 8; i++) {
        
    }

    cout << ans << '\n';

    return 0;
}